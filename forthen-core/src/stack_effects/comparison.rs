
use super::element::ElementRef;

pub fn is_sequence_equivalent(a: &[ElementRef], b: &[ElementRef]) -> bool {
    //a.len() == b.len() && a.iter().zip(b).all(|(ae, be)| ae.is_equivalent(be))
    unimplemented!()
}
